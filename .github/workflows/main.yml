# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run.
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [main]
  pull_request:
    branches: [main]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      #   - uses: actions/hello-world-javascript-action@main
      #     with:
      #       who-to-greet: 'Mona the Octocat'

      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      - name: Setup js/ts environment
        uses: ./actions/hello-world-composite
        with:
          who-to-greet: 'Mona the Octocat'
      # test javascript action
      # - name: Run custom JS action
      #   uses: ./actions/hello-world-js-action
      #   with:
      #     who-to-greet: 'Mona the Octocat'

      # - name: Run custom JS action 2
      #   uses: ./actions/hello-world-2-js-action

      # Test hash and file access
      # - run: echo hasfile1 ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}-${{ hashFiles('.github/workflows/**.yml') }}
      #   shell: bash
      #   name: From workflow

      # Run composite run action
      # - uses: ./actions/hello-world-composite-run-steps-action
      #   name: Run custom action
      #   with:
      #     who-to-greet: 'random-dude-5342'

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
